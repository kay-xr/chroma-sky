// Config file for chroma-sky

export const config = {
	username: 'bsky.app', // Enter your bsky @
	includeReplies: false, // Include your replies to posts
	postsPerPage: 100, // Default amount of images per page, the higher this number, the longer the loading time (max 100)
	keywords: [] // Array of keywords to search for posts, leave blank '[]' to show all photo posts
}